sample:
  name: Pouch HTTP Client
common:
  harness: pytest
  timeout: 180
tests:
  pouch.http_client.sample:
    harness_config:
      pytest_dut_scope: module
      pytest_root:
        - pytest/test_sample.py
      pytest_args:
        - --ota-mode=dummy
    platform_allow:
      - native_sim
      - native_sim/native/64
    extra_configs:
      - CONFIG_FILE_SYSTEM_NSIM_MOUNT=y
      - 'CONFIG_NATIVE_EXTRA_CMDLINE_ARGS="-volume=./creds:/creds"'
      - 'CONFIG_EXAMPLE_CREDENTIALS_DIR="/creds"'
      - CONFIG_EXAMPLE_HTTP_CLIENT_SYNC_PERIOD_S=2
      - CONFIG_EXAMPLE_FW_UPDATE_SHA256_VERIFY=y
      - CONFIG_GOLIOTH_OTA_MAX_VERSION_LEN=64
      - 'CONFIG_EXAMPLE_FW_UPDATE_COMPONENT="ci_ota_fw"'
  pouch.http_client.hil:
    harness_config:
      pytest_dut_scope: module
      pytest_root:
        - pytest/test_sample.py
    extra_configs:
      - CONFIG_EXAMPLE_HTTP_CLIENT_SYNC_PERIOD_S=10
    platform_allow:
      - frdm_rw612
