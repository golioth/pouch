CONFIG_ZTEST=y

# We compile selected src/gateway sources into the test app directly
# (see CMakeLists.txt) so we don't have to enable the full POUCH_GATEWAY
# Kconfig and pull in BT / SAR / broker endpoint dependencies.  Settings
# expected by those sources (log level, block size knobs) are provided
# as -D macros from CMakeLists.txt.
CONFIG_LOG=y
